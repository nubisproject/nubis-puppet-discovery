{
  "service": {
    "name": "<%= @name  %>",
    "tags": <%= @all_tags.to_json %>,
<% if @port %>
    "port": <%= @port %>,
<% end %>
    "check": {
      "script": "<%= @check %>",
      "interval": "<%= @interval %>"
    }
  }
}
