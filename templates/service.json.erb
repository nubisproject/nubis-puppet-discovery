{
  "service": {
    "name": "<%= @name  %>",
    "tags": <%= @tags.to_json %>,
    "port": <%= @port %>,
    "check": {
      "script": "<%= @check %>",
      "interval": "<%= @interval %>"
    }
  }
}
