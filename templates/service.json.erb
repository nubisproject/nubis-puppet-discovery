{
  "service": {
    "name": "<%= @name  %>",
    "tags": <%= @tags.to_json %>,
    "port": <%= @port %>,
    "checks": [
      {
        "script": "<%= @check %>",
        "interval": "<%= @interval %>"
      }
    ]
  }
}
